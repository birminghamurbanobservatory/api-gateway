import * as check from 'check-types';



// This is for when you get a load of resources, e.g. platforms, but you can't guaretee that you'll be able to get them all, for those that you can't get we'll just create an object for it with just the id in it, e.g. {id: 'platform-1'}
export function populateMultiple(ids: string[], objs: object[]): {

  

}